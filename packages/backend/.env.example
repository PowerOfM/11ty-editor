# Firebase project (used by the functions runtime)
GCLOUD_PROJECT=your-firebase-project-id

# Restrict CORS to your deployed frontend URL
ALLOWED_ORIGIN=https://your-editor.web.app

# Optional: restrict which GitHub users can use the editor
# (comma-separated GitHub usernames)
ALLOWED_GITHUB_USERS=
